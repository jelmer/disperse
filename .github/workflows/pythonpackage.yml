---
name: Python package

"on": [push, pull_request]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.9", "3.10"]
      fail-fast: false

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          sudo apt install protobuf-compiler
          python -m pip install --upgrade setuptools setuptools-protobuf
          python -m pip install --upgrade pip . mypy-protobuf
          python setup.py develop
          protoc --python_out=. --mypy_out=. disperse/config.proto
      - name: Style checks (flake8)
        run: |
          python -m pip install --upgrade flake8
          python -m flake8
      - name: Style checks (yaml)
        run: |
          python -m pip install --upgrade yamllint
          yamllint .
      - name: Typing checks
        run: |
          pip install -U mypy types-toml
          python -m mypy disperse
        if: "matrix.python-version != 'pypy3'"
      - name: Tests
        run: |
          python -m unittest disperse.tests.test_suite
      - name: Validate disperse.conf
        run: |
          disperse validate .
        env:
          PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION: python
